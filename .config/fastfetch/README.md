# Fastfetch logo

Replace `logo.webp` with your respective distro image (any format supported by your terminal's image protocol).

Then update the `source` path in `config.jsonc` to match the new filename.
